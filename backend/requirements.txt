fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.12,<3
pydantic-settings==2.5.2

# OpenAI-compatible client; also used against OpenRouter, Groq and local Ollama/vLLM.
openai>=1.40.0

# Analytics store. App state lives in SQLite via the stdlib sqlite3 module.
duckdb==1.1.1

# Skill definitions are YAML frontmatter in SKILL.md.
PyYAML==6.0.2

PyJWT==2.9.0
bcrypt==4.2.0

# Ingestion sources.
openpyxl==3.1.5
google-api-python-client==2.147.0
google-auth==2.35.0

# Tests.
pytest==8.3.3

# Charts are rendered client-side from chart content blocks, so no server-side plotting
# library is needed. Guardrails use a built-in rule set rather than llm-guard.
